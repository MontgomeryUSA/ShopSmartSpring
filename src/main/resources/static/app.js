document.addEventListener("DOMContentLoaded", () => { const quotesDiv = document.getElementById("quotes"); fetch("/api/quotes") .then(response => response.json()) .then(quotes => { quotes.forEach(quote => { const quoteDiv = document.createElement("div"); quoteDiv.className = "quote"; quoteDiv.innerHTML = `<p>${quote.text}</p><p><strong>- ${quote.author}</strong></p>`; quotesDiv.appendChild(quoteDiv); }); }) .catch(error => console.error("Error fetching quotes:", error)); });